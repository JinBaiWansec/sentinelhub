# 设计说明

这个项目是按真实 SaaS 的样子搭的:监控、告警、计费、通知、报告一整套功能,分层架构,业务规则分散在各自的模块里。写这份说明,是想讲清楚代码为什么长这样。

## 为什么业务逻辑放在 service 层

路由层在真实项目里只是一层皮,真正干活的是 service 层。视图函数只解析请求、调用服务、返回结果,业务规则沉淀在服务层,数据访问收敛在数据层。这样拆,改业务不用动路由,加接口不用复制逻辑。读代码时顺着这条线走,就能把每个功能的数据流看全。

## 为什么敏感操作前面都有前置条件

真实产品里,"登录了"不等于"什么都能做"。角色决定权限边界,套餐决定功能范围,跨实例的数据要做完整性校验,异步任务要有来源验证。这些限制是产品规则的一部分,不是额外贴上去的补丁——它们让每个操作停留在它该在的边界里。

## 为什么历史功能和预览被"关"起来

升级过系统的都知道,老的模板格式要兼容,所以旧的渲染方式留着,新功能走新路径。重一点的导出操作不能卡着请求,做成异步、带签名。用户能输入的模板,预览一律放进沙箱,不让它碰系统。这些是工程上的常规取舍,合在一起,就是产品正常运转的样子。

## 为什么各模块各管各的

监控、计费、通知、报告、团队、集成,各自管自己的数据,模块间只通过服务层互相调用,不直接动对方的表。边界清楚,改动互不牵连,这是产品能长期维护的前提。对读代码的人来说,也能按模块一个个排查,不用一次装下整个项目。
